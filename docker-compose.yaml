services:
  simple-apps:
    container_name: ct-simpleapps
    images: mreuy/img-simpleapps
    ports:
    - "10000:3000"
    restart: always
    deploy:
      resources:
        limits:
          cpus: "0.5"
          memory: 128M